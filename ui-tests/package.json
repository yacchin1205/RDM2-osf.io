{
  "name": "ui-tests",
  "version": "1.0.0",
  "description": "UI tests for OSF with Playwright",
  "scripts": {
    "test": "npx playwright test",
    "test:headed": "npx playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}